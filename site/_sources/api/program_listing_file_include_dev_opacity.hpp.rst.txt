
.. _program_listing_file_include_dev_opacity.hpp:

Program Listing for File opacity.hpp
====================================

|exhale_lsh| :ref:`Return to documentation for file <file_include_dev_opacity.hpp>` (``include/dev/opacity.hpp``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   #pragma once
   
   #include <vector>
   #include <string>
   
   // ================================
   // Opacity table
   // ================================
   struct OpacityTable {
       double X = 0;
       double Y = 0;
       double Zbase = 0;
       double dXC = 0;
       double dXO = 0;
   
       std::vector<double> log_r;
       std::vector<double> log_t;
       std::vector<std::vector<double>> grid;
   
       bool contains(double logT, double logR) const;
   
       double interpolate(double logT, double logR) const;
   };
   
   
   const OpacityTable& selectTable(
       const std::vector<OpacityTable>& tables,
       double Y,
       double dXC,
       double dXO,
       double logT,
       double logR
   );
   
   // ================================
   // API
   // ================================
   
   // load opacity tables from file -- hardcoded path for now
   std::vector<OpacityTable> loadOpacityTables();
   
   // optional: quick test
   bool testOpacityLoad();
